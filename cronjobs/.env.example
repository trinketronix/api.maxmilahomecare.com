# Copy to cronjobs/.env on the server (outside the web root) and fill in the values.
# The API's Apache SetEnv values are not visible to cron jobs.
DB_HOSTPATH=localhost
DB_DATABASE=maxmilah_test
DB_USERNAME=
DB_PASSWORD=
# DB_PORT=3306
